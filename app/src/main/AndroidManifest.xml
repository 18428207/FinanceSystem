<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.administrator.financesystem">

   <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".A1_LogRe">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".A2_Login"></activity>
        <activity android:name=".A3_Register"></activity>
        <activity android:name=".E1_WealthIndex"></activity>
       <activity android:name=".C1_FriendsIndex"></activity>
       <activity android:name=".C2_SearchFriend"></activity>
    </application>

</manifest>